#include "funkcie.h"
#include <stdio.h>
#include <unistd.h>
int main (int argc , char *argv[])
{
	int a,b;
	char *meno_izby;
	pid_t PID1,PID2;
	sscanf();
	sscanf();
	PID1=fork();

      if(PID1 > 0) PID2=fork();
	if(PID1==0);
	{
           nasobenie();
	}











}
